//Criar um programa para verificar se um número é par ou ímpar, usando OPERADOR TERNÀRIO.
let numero = 10;
let resultado = (numero % 2 === 0) ? "Par" : "Ímpar";
console.log(`${numero} é ${resultado}.`);